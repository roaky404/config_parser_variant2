# Пример конфигурации
server_count <- 3;
timeout <- 30;

begin
  servers := array(
    @"api1.example.com",
    @"api2.example.com",
    @"api3.example.com"
  );
  max_connections := ${* server_count 100};
  timeout_seconds := timeout;
end